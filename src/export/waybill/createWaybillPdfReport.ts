import puppeteer, { PDFOptions } from "puppeteer";
import handlebars from "handlebars";
import path from "path";
import fs from "fs";

export const createWaybillPdfReport = (waybill: any) => {
  console.log("HELLO from npm");
};
